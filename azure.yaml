# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: remote-mcp-functions-dotnet
metadata:
  template: remote-mcp-functions-dotnet@1.0.0
services:
  api:
    project: ./src/
    language: dotnet
    host: function
